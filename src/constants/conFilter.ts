import type { IFilterOption } from '@/types/news.types';

export const CON_NEWS_SEARCH_FILTER_OPTIONS: IFilterOption[] = ['category', 'country', 'pageSize'];
export const CON_NEWS_SOURCE_FILTER_OPTIONS: IFilterOption[] = ['category', 'country', 'groupBy'];
